- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.tmux.conf: tmux.conf
    ~/.emacs: emacs
    ~/.bashrc: bashrc
    ~/.profile: profile

- shell:
  - [git submodule update --init --recursive, Installing submodules]

